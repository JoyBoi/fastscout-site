---
export const prerender = false;

import Layout from "../../shared/Layout.astro";
import LoadingButton from "../../components/LoadingButton.astro";
import stripe, { findActiveSubscriptionByEmail, findActiveSubscriptionByCustomerId } from "../../lib/stripe";
import { getAuthenticatedUser } from "../../lib/auth";
import { getConvexClient } from "../../lib/convex";
import { api } from "../../../convex/_generated/api";
import { locales, getLocaleFromUrl, useTranslations } from "../../i18n";



const locale = getLocaleFromUrl(Astro.url);
const t = useTranslations(locale);
const localePath = (path: string) => `/${locale}${path}`;

let monthlyId = import.meta.env.STRIPE_PRICE_ID_MONTHLY as string | undefined;
const yearlyId = import.meta.env.STRIPE_PRICE_ID_YEARLY as string | undefined;
const fallbackId = import.meta.env.STRIPE_PRICE_ID as string | undefined;
if (!monthlyId && fallbackId) monthlyId = fallbackId;
const sessionUser = getAuthenticatedUser(Astro.request);
const userId = sessionUser?.userId as string | undefined;
const userEmail = sessionUser?.email as string | undefined;
const urlError = new URL(Astro.request.url).searchParams.get("error");
let errorBanner: string | null = null;
if (urlError === "rate_limited") errorBanner = t.pricing.errors.rateLimited;
else if (urlError === "checkout_failed") errorBanner = t.pricing.errors.checkoutFailed;
else if (urlError === "missing_price_id") errorBanner = t.pricing.errors.missingPrice;
else if (urlError === "already_subscribed") errorBanner = t.pricing.errors.alreadySubscribed;
let subActive = false;
let subPriceId: string | undefined;

// Fetch subscription and billing data from Convex in parallel
const convex = getConvexClient();
const [status, billing] = await Promise.all([
  userId
    ? convex.query(api.subscriptions.getByUserId, { userId })
    : Promise.resolve(null),
  userId
    ? convex.query(api.billingCustomers.getByUserId, { userId })
    : Promise.resolve(null),
]);

if (status) {
  subActive = !!status.active;
  subPriceId = status.priceId ?? undefined;
}

if (!subActive && userEmail && userId) {
  const customerId = billing?.stripeCustomerId as string | undefined;
  if (customerId) {
    const res = await findActiveSubscriptionByCustomerId(customerId);
    subActive = res.active;
    subPriceId = subPriceId ?? res.priceId;
  } else {
    const res = await findActiveSubscriptionByEmail(userEmail);
    subActive = res.active;
    subPriceId = subPriceId ?? res.priceId;
  }
}

const quarterlyId = import.meta.env.STRIPE_PRICE_ID_QUARTERLY as string | undefined;
const halfyearlyId = import.meta.env.STRIPE_PRICE_ID_HALFYEARLY as string | undefined;
---
<Layout title={t.pricing.title} locale={locale}>
  <section style="max-width: 760px; margin: 0 auto;">
    <div style="text-align: center; margin-bottom: 48px;">
      <span class="badge" style="margin-bottom: 16px; display: inline-block;">Simple Pricing</span>
      <h2 style="font-family: 'Syne', sans-serif; font-size: clamp(28px, 3.5vw, 40px); font-weight: 800; letter-spacing: -0.02em; margin-bottom: 12px; color: var(--color-text);">{t.pricing.title}</h2>
      <p style="font-size: 14px; color: var(--color-text-muted); max-width: 460px; margin: 0 auto; line-height: 1.6;">{t.pricing.subtitle}</p>
    </div>

    <style is:inline>
      .pricing-grid {
        display: grid;
        grid-template-columns: repeat(2, 1fr);
        gap: 24px;
        max-width: 760px;
        margin: 0 auto;
      }

      .plan {
        padding: 32px;
        display: flex;
        flex-direction: column;
        height: 100%;
        border-radius: var(--radius-lg);
        transition: all 0.35s cubic-bezier(0.16, 1, 0.3, 1);
      }

      .plan:hover {
        transform: translateY(-4px);
      }

      .plan--featured {
        border: 2px solid transparent;
        background: linear-gradient(var(--color-bg-card), var(--color-bg-card)) padding-box,
                    var(--gradient-primary) border-box;
        box-shadow: 0 16px 32px -8px rgba(6, 182, 212, 0.12);
        position: relative;
      }

      .plan--featured::before {
        content: '';
        display: block;
        position: absolute;
        top: 0; left: 0; right: 0;
        height: 3px;
        background: var(--gradient-primary);
        border-radius: var(--radius-lg) var(--radius-lg) 0 0;
      }

      .plan-header { margin-bottom: 24px; }

      .plan-header h3 {
        font-family: 'Syne', sans-serif;
        font-size: 17px;
        font-weight: 600;
      }

      .price-tag {
        font-family: 'Syne', sans-serif;
        font-size: 36px;
        font-weight: 800;
        color: var(--color-text);
        line-height: 1;
        letter-spacing: -0.03em;
      }

      .price-period {
        font-size: 14px;
        color: var(--color-text-muted);
        font-weight: 500;
      }

      .plan-desc {
        font-size: 13px;
        color: var(--color-text-muted);
        margin-top: 10px;
        line-height: 1.5;
      }

      .plan-annual {
        margin-top: 6px;
        font-size: 12px;
        color: var(--color-primary);
        font-weight: 600;
        line-height: 1.5;
      }
      .plan-annual s {
        color: var(--color-text-muted);
        font-weight: 400;
      }

      .feature-list { margin: 24px 0; flex-grow: 1; }
      .feature-list li { margin-bottom: 14px; }

      .plan-actions {
        margin-top: auto;
        display: flex;
        flex-direction: column;
        gap: 8px;
      }
      .plan-actions form { width: 100%; }

      @media (max-width: 768px) {
        .pricing-grid { grid-template-columns: 1fr; max-width: 380px; }
      }
    </style>

    {
      errorBanner ? (
        <div class="card" style="border-color: rgba(239,68,68,0.4); background: rgba(239,68,68,0.08); margin-bottom:24px; text-align:center">
          <strong style="color:#ef4444">{t.common.error}</strong>
          <p style="margin-top:6px; font-size: 14px; color: var(--color-text-muted);">{errorBanner}</p>
          {
            urlError === "already_subscribed" ? (
              <div style="margin-top:10px">
                <a class="btn" href={localePath('/dashboard')}>{t.nav.dashboard}</a>
              </div>
            ) : null
          }
        </div>
      ) : null
    }

    <div class="pricing-grid">
      <!-- Starter Plan -->
      <div class="card plan">
        <div class="plan-header">
          <h3 style="margin-bottom: 8px;">Starter</h3>
          <div style="display:flex; align-items:baseline; gap:6px">
            <span class="price-tag">50&euro;</span>
            <span class="price-period">/mois</span>
          </div>
          <p class="plan-desc">Jusqu'a 500 vehicules par mois.</p>
          <p class="plan-annual">Annuel : 510&euro;/an <s>600&euro;</s> <span class="badge">-15%</span></p>
        </div>

        <ul class="feature-list">
          <li>500 vehicules / mois</li>
          <li>Chrome Extension</li>
          <li>Dashboard complet</li>
          <li>Support standard</li>
        </ul>

        <div class="plan-actions">
        {
          subActive && subPriceId === monthlyId ? (
            <button class="btn btn-outline" style="width: 100%;" disabled>Plan actuel</button>
          ) : (
            <form action="/api/checkout" method="POST">
              <input type="hidden" name="plan" value="monthly" />
              <input type="hidden" name="locale" value={locale} />
              <LoadingButton variant="outline" style="width: 100%;">Choisir Starter mensuel</LoadingButton>
            </form>
          )
        }
        {
          subActive && subPriceId === yearlyId ? (
            <button class="btn btn-outline" style="width: 100%;" disabled>Plan actuel</button>
          ) : (
            <form action="/api/checkout" method="POST">
              <input type="hidden" name="plan" value="annual" />
              <input type="hidden" name="locale" value={locale} />
              <LoadingButton variant="outline" style="width: 100%;">Choisir Starter annuel</LoadingButton>
            </form>
          )
        }
        </div>
      </div>

      <!-- Pro Plan (Featured) -->
      <div class="card plan plan--featured">
        <div class="plan-header">
          <div style="display:flex; justify-content:space-between; align-items:center; margin-bottom: 8px;">
            <h3 style="margin:0;">Pro</h3>
            <span class="badge">Illimite</span>
          </div>
          <div style="display:flex; align-items:baseline; gap:6px">
            <span class="price-tag">250&euro;</span>
            <span class="price-period">/mois</span>
          </div>
          <p class="plan-desc">Vehicules illimites, pour les pros.</p>
          <p class="plan-annual">Annuel : 2 550&euro;/an <s>3 000&euro;</s> <span class="badge">-15%</span></p>
        </div>

        <ul class="feature-list">
          <li><strong>Vehicules illimites</strong></li>
          <li>Chrome Extension</li>
          <li>Dashboard complet</li>
          <li>Support prioritaire</li>
          <li>Acces anticipe aux nouveautes</li>
        </ul>

        <div class="plan-actions">
        {
          subActive && subPriceId === quarterlyId ? (
            <button class="btn btn-primary" style="width: 100%;" disabled>Plan actuel</button>
          ) : (
            <form action="/api/checkout" method="POST">
              <input type="hidden" name="plan" value="quarterly" />
              <input type="hidden" name="locale" value={locale} />
              <LoadingButton variant="primary" style="width: 100%;">Choisir Pro mensuel</LoadingButton>
            </form>
          )
        }
        {
          subActive && subPriceId === halfyearlyId ? (
            <button class="btn btn-primary" style="width: 100%;" disabled>Plan actuel</button>
          ) : (
            <form action="/api/checkout" method="POST">
              <input type="hidden" name="plan" value="halfyearly" />
              <input type="hidden" name="locale" value={locale} />
              <LoadingButton variant="primary" style="width: 100%;">Choisir Pro annuel</LoadingButton>
            </form>
          )
        }
        </div>
      </div>
    </div>

    <div class="card" style="margin-top: 48px; text-align: left; padding: 32px;">
      <h3 style="font-family: 'Syne', sans-serif; font-size: 18px; font-weight: 700; margin-bottom: 24px;">{t.pricing.faq.title}</h3>
      <div class="grid grid-2" style="gap: 24px;">
        <div>
          <h4 style="font-size: 14px; font-weight: 600; margin-bottom: 6px; color: var(--color-text);">{t.pricing.faq.cancel.question}</h4>
          <p class="muted" style="font-size: 13px; line-height: 1.7;">{t.pricing.faq.cancel.answer}</p>
        </div>
        <div>
          <h4 style="font-size: 14px; font-weight: 600; margin-bottom: 6px; color: var(--color-text);">{t.pricing.faq.invoices.question}</h4>
          <p class="muted" style="font-size: 13px; line-height: 1.7;">{t.pricing.faq.invoices.answer}</p>
        </div>
      </div>
    </div>

    <div style="display: flex; justify-content: center; gap: 32px; margin-top: 36px; flex-wrap: wrap;">
      <div style="display: flex; align-items: center; gap: 6px; color: var(--color-text-dim); font-size: 12px; font-weight: 500;">
        <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"/></svg>
        Secure payments via Stripe
      </div>
      <div style="display: flex; align-items: center; gap: 6px; color: var(--color-text-dim); font-size: 12px; font-weight: 500;">
        <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        Cancel anytime
      </div>
      <div style="display: flex; align-items: center; gap: 6px; color: var(--color-text-dim); font-size: 12px; font-weight: 500;">
        <svg width="16" height="16" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2"><path stroke-linecap="round" stroke-linejoin="round" d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"/></svg>
        No hidden fees
      </div>
    </div>
  </section>
</Layout>
